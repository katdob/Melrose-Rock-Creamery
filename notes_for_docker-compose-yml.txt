# Build and start all services
docker-compose up -d

# View logs
docker-compose logs -f

# Stop all services
docker-compose down

# Stop and remove volumes (clean slate)
docker-compose down -v


# NUKE everything 
docker-compose down -v && docker system prune -a -f && docker-compose up -d --build

# MRC_API ↔ PostgreSQL connection
# See docs/MRC_API_DATABASE_SETUP.md for Dockerfile changes (Csharp_dotNET_backend, PostgreSQL_DB) and connection details.